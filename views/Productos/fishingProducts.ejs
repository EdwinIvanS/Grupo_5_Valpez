<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
    <link href="/css/styles_home.css" rel="stylesheet">
    <title>.:Pesca:.</title>
</head>

<body>
    <!-- Seccion de la cabecera-->
    <%- include('../partials/header') %>

    <!-- Seccion del cuerpo-->
    <main>
        <div class="related">
            <h2 class="titleSub">Productos de Pesca</h2>

            <div class="relatedProducts">
                <% for(let i=0 ; i < fishingProducts.length ; i ++) {%>                        
                            <article class="product">
                                <a href="/products/detail/<%= fishingProducts[i].idProduct %>">
                                    <img src="/img/Products/<%= fishingProducts[i].images[0] %>" alt="producto5">
                                </a>
                                <h3>
                                    <%= fishingProducts[i].title%>
                                </h3>
                                <p>
                                    <%= fishingProducts[i].price%>
                                </p>
                                <form class="selection1" action="#" method="get">
                                    <div>
                                        <select name="color" id="color">
                                            <% for (let c = 0; c < fishingProducts[i].colors.length; c++) {%>
                                                <option value="<%= fishingProducts[i].colors[c]%>"><%= fishingProducts[i].colors[c]%></option>
                                                <% } %>
                                        </select>
                
                                        <select name="quantity" id="quantity">
                                            <% for (let q = 1; q <= fishingProducts[i].units; q++) {%>
                                                <option value="<%= q %>"><%= q %></option>
                                                <% } %>
                                        </select>
                                    </div>
                                    <div>
                                        <select name="size" id="size">
                                            <% for (let s = 0; s < fishingProducts[i].size.length; s++) {%>
                                                <option value="<%= fishingProducts[i].size[s]%>"><%= fishingProducts[i].size[s]%></option>
                                                <% } %>
                                        </select>
                
                                        <button type="submit"><i class="fa-solid fa-cart-plus"></i></button>
                                    </div>
                                </form>
                            </article>                        
                <% } %>
            </div>
        </div>
    </main>

    <!-- Seccion del pie de pagina-->
    <%- include('../partials/footer') %>

</body>

</html>